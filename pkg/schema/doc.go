// Package schema defines the canonical data model used by dahash.
//
// The schema is intentionally data-first. The runtime loads HashType records
// from dahash JSON data instead of hardcoding individual hash formats in Go.
package schema
