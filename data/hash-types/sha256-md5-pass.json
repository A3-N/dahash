{
  "schema_version": "dahash.hash_type/v2",
  "id": "sha256-md5-pass",
  "name": "sha256(md5($pass))",
  "aliases": [
    "sha256(md5($pass))"
  ],
  "status": "active",
  "priority": 50,
  "extraction": {
    "supported": false,
    "inputs": [],
    "converters": []
  },
  "tools": {
    "hashcat": [
      {
        "mode": 20800,
        "name": "sha256(md5($pass))",
        "status": "active"
      }
    ]
  },
  "match": {
    "base": 25,
    "total": 95,
    "requires": [
      {
        "feature": "regex",
        "args": {
          "regex": "^[A-Fa-f0-9]{64}$"
        },
        "id": "required_pattern"
      }
    ],
    "evidence": [
      {
        "feature": "regex",
        "args": {
          "regex": "^[A-Fa-f0-9]{64}$"
        },
        "id": "pattern_match",
        "weight": 70
      }
    ]
  }
}
