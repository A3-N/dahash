{
  "schema_version": "dahash.hash_type/v2",
  "id": "gpg-aes-128-aes-256-sha-256-pass",
  "name": "GPG (AES-128/AES-256 (SHA-256($pass)))",
  "aliases": [
    "GPG (AES-128/AES-256 (SHA-256($pass)))"
  ],
  "status": "active",
  "priority": 50,
  "extraction": {
    "supported": false,
    "inputs": [],
    "converters": [
      {
        "tool": "john",
        "name": "gpg2john",
        "args": [
          "{input}"
        ],
        "outputs": [
          "$gpg$"
        ],
        "input_role": "direct"
      }
    ]
  },
  "tools": {
    "hashcat": [
      {
        "mode": 17030,
        "name": "GPG (AES-128/AES-256 (SHA-256($pass)))",
        "status": "active"
      }
    ]
  },
  "identification": {
    "max_confidence": 75
  },
  "match": {
    "base": 25,
    "total": 140,
    "requires": [
      {
        "feature": "regex",
        "args": {
          "regex": "^\\$gpg\\$\\*[0-9]+\\*[0-9]+\\*[0-9]+\\*[A-Fa-f0-9]{128,}(?:\\*[0-9A-Fa-f]+){8}$",
          "flags": [
            "i"
          ]
        },
        "id": "pattern_match"
      }
    ],
    "evidence": [
      {
        "feature": "regex",
        "args": {
          "regex": "^\\$gpg\\$\\*[0-9]+\\*[0-9]+\\*[0-9]+\\*[A-Fa-f0-9]{128,}(?:\\*[0-9A-Fa-f]+){8}$",
          "flags": [
            "i"
          ]
        },
        "id": "pattern_match",
        "weight": 70
      },
      {
        "feature": "prefix",
        "args": {
          "value": "$gpg$"
        },
        "id": "marker_prefix",
        "weight": 45
      }
    ]
  }
}
